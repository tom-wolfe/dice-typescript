import * as Ast from "../ast";

export class Parser {
    parse(input: string): Ast.Expression {
        const root: Ast.ExpressionNode = null;

        // TODO: Implement.

        return new Ast.Expression(root);
    }
}
